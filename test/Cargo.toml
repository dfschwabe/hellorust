[package]
name = "it"
version = "0.0.0"

[dependencies]

[[bin]]
name = "it"
path = "it.rs"

[package.metadata.docs.rs]
features = ["nightly"]

[features]
# Extra features for the nightly compiler only
nightly = []
